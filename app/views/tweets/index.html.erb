<p id="notice"><%= notice %></p>

<h1>Tweets</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tweets.each do |tweet| %>
      <tr>
        <td><%= tweet.content %></td>
        <td><%= link_to 'Show', user_tweet_path(current_user, tweet) %></td>
        <td><%= link_to 'Edit', edit_user_tweet_path(current_user, tweet) %></td>
        <td><%= link_to 'Destroy', user_tweet_path(current_user, tweet), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tweet', new_user_tweet_path(current_user) %>
